{
  "name": "@rebelcode/plugin-injectable",
  "version": "0.0.1",
  "description": "Adds injection markers and parameters list to instances marked as injectable",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "RebelCode",
  "license": "MIT",
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-cli": "^6.26.0",
    "babel-plugin-example": "^1.0.0"
  }
}
